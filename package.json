{
  "name": "dfg-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "partykit dev --port 1999",
    "deploy": "partykit deploy",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "partysocket": "^1.0.2"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^29.7.0",
    "partykit": "^0.0.114",
    "ts-jest": "^29.4.1",
    "typescript": "^5.5.4"
  }
}
